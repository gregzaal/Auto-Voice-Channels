/**
 * The `""mode:text""` string transforms moved to `@avc/core/template` with the
 * rest of the engine. Kept as a re-export so the
 * assistant's validator and the settings code keep their existing imports.
 */
export * from '@avc/core/template';
